const sql = require("./db.js");

// constructor
const Level_user = function(level_user) {
    this.id = level_user.id;
    this.unlocked  = level_user.unlocked;
};

Level_user.create = (newLevel, result) => {
  sql.query("INSERT INTO level_user SET ?", newLevel, (err, res) => {
    if (err) {
      console.log("error: ", err);
      result(err, null);
      return;
    }

    console.log("created user: ", { id: res.insertId, ...newLevel });
    result(null, { id: res.insertId, ...newLevel });
  });
};

Level_user.findById = (id, result) => {
  sql.query(`SELECT * FROM level_user WHERE id = ${id}`, (err, res) => {
    if (err) {
      console.log("error: ", err);
      result(err, null);
      return;
    }

    if (res.length) {
      console.log("found user: ", res[0]);
      result(null, res[0]);
      return;
    }

    // not found User with the id
    result({ kind: "not_found" }, null);
  });
};

Level_user.getAll = (title, result) => {
  let query = "SELECT * FROM level_user";

  if (title) {
    query += ` WHERE title LIKE '%${title}%'`;
  }

  sql.query(query, (err, res) => {
    if (err) {
      console.log("error: ", err);
      result(null, err);
      return;
    }

    console.log("users: ", res);
    result(null, res);
  });
};

Level_user.updateById = (id, level_user, result) => {
  sql.query(
    "UPDATE level_user SET unlocked = ? WHERE id = ?",
    [ user.username, user.password, id],
    (err, res) => {
      if (err) {
        console.log("error: ", err);
        result(null, err);
        return;
      }

      if (res.affectedRows == 0) {
        // not found User with the id
        result({ kind: "not_found" }, null);
        return;
      }

      console.log("updated user: ", { id: id, ...user });
      result(null, { id: id, ...user });
    }
  );
};

Level_user.remove = (id, result) => {
  sql.query("DELETE FROM level_user WHERE id = ?", id, (err, res) => {
    if (err) {
      console.log("error: ", err);
      result(null, err);
      return;
    }

    if (res.affectedRows == 0) {
      // not found User with the id
      result({ kind: "not_found" }, null);
      return;
    }

    console.log("deleted user with id: ", id);
    result(null, res);
  });
};

Level_user.removeAll = result => {
  sql.query("DELETE FROM level_user", (err, res) => {
    if (err) {
      console.log("error: ", err);
      result(null, err);
      return;
    }

    console.log(`deleted ${res.affectedRows} users`);
    result(null, res);
  });
};

module.exports = User;
